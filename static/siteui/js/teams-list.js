// static/siteui/js/teams-list.js
(function () {
  const form = document.getElementById('teams-search-form');
  if (form) {
    form.addEventListener('submit', function (e) {
      // Let the browser submit normally — query params persist.
    });
  }
})();
