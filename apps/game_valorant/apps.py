from django.apps import AppConfig

class GameValorantConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.game_valorant"
    verbose_name = "Valorant (Config)"
