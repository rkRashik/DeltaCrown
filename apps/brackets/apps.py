from django.apps import AppConfig


class BracketsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.brackets"
    label = "brackets"
    verbose_name = "Brackets"
